# Bind PowerDNS only to the LAN IP
PDNS_BIND_IP=replace-this-with-a-local-ip-address

# MySQL root password
MYSQL_ROOT_PASSWORD=change-me

# MySQL password for normal user
DB_PASSWORD=change-me

# PowerDNS API Key
API_KEY=replace-this-with-a-long-random-string
